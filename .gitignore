# macOS system files
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Python
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
*.pyc

# Virtual environments
venv/
env/
ENV/
.venv/
.Python

# Django
*.log
*.pot
*.pyc
db.sqlite3
local_settings.py

# Media and static (exclude media uploads, allow collected static for production)
media/
# Ignore collected staticfiles ONLY in local dev (Heroku uses collectstatic)
# Uncomment if you want to keep staticfiles tracked locally
# staticfiles/

# Environment variables
.env
.env.*

# Migrations — track all except compiled or temporary
# Allows real migration files into version control
# You may adjust this if you want to exclude migration history
# but it's safer to track them
# DO NOT ignore real migration files
# (this block intentionally removed)

# VS Code
.vscode/
.code-workspace

# JetBrains (PyCharm)
.idea/

# Node (if used for frontend tools)
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log

# Coverage and testing
.coverage
coverage.xml
htmlcov/
*.cover
.tox/
.pytest_cache/
nosetests.xml
test_db.sqlite3

# Build artifacts
build/
dist/
*.egg-info/
*.egg

# Bytecode cache
*.bak
*.swp
*.swo

# Lock files
*.lock
